type Int;
type Ptr;
type Void;

extern printf(qword str:Ptr):Void;

func square(dword n:Int):Int {
	return n*n;
}

/* entry point */
func _start():Int {
	dword a:Int = 5;
	square(a);
	printf(1.2);
	return 0;
} global _start;
