type int;
type void;
type ptr;
type char;

func print(qword a:ptr):void {
	/* not implemented */
}
func itoc(qword i:int):char {
	return $(qword)@i;
}

dword a:int = 0;
while(a < 50) {
	a += 1;
	qword num:char = itoc(a);
	print(@num);
	while(a < 100) {
		a+=10;
		num = itoc(a);
		print(@num);
	}
}
print(@"program done");
